# 测试（tests/）

> 测试开发位置。按契约与验收标准回归。

---

## 契约测试（对应 `api-contract.md`）
- [ ] `save` 空文本拒绝（`text-blank`）
- [ ] `remove` 幂等
- [ ] `ask` 不加上下文时**不读会话**
- [ ] 错误码信封结构一致

## 回归（对应 `requirements.md` 验收 AC）
- [ ] AC-1 选区收藏 → 持久化（刷新后仍在）
- [ ] AC-2 抽屉列/删
- [ ] AC-3 单条 + 不加上下文追问
- [ ] AC-4 多条 + 自定义问题 + 加上下文追问
- [ ] AC-5 卸载无残留

## 注入器全链路自检
- 改完代码跑 `dev_self_test`，确认注入 / 重载 / 自重载 / 预检 / 卸载不退化。

## 说明
- 当前已补充 `contract.test.mjs`，覆盖文本规范化、首次收藏、追加收藏、删除幂等和最后一条删除。服务集成与浏览器 E2E 仍按 M1/M2 逐步补充。
