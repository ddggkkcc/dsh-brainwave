# 当前需要处理的问题（Open Questions）

> 这些问题**阻塞设计 / 实现**，需要先拍板。每项给出候选与推荐。
> 2026-08-20 更新：**全部待决项已拍板**（Q2~Q5 用户确认，Q6/Q7 暂按推荐）。

---

## Q1：命名 —— ✅ 已定
- **决议**：插件名「**随手抽屉**」（英文 **Stash**），存储地叫「**抽屉**」，包名 `dsh-stash`。
- **原文**：原文写「灵感库，还没想好如何命名」——现已定名为「抽屉」。
- **备注**：浮窗显示文案可后续微调（如「临时追问 / 快问」），不影响结构。

## Q2：「加入上下文」的上下文来源 —— ✅ 已定（2026-08-20）
- **语义（用户原话确认）**：
  - **加入上下文**：结合前面表述的上下文，从对话语境的角度解释追问内容；
  - **不加入上下文**：独立回答，只解释所选内容的含义；
  - **两种模式都不写入主会话**。
- **实现决议**：加入分支走「宿主侧解析当前会话消息」（原候选 A，ADR-003 已接受）；**M1 做最小版**（读最近 N 条消息 + 截断，见 R6），M2 完善（更长会话策略 / 摘要 / 流式）。原候选 C（仅选中文本 + 手动追加）降级为兜底，不作为语义目标。

## Q3：「临时追问」的深度 —— ✅ 已定（2026-08-20）
- **决议**：MVP 用**一次性无工具问答**（`ctx.llm.stream`，ADR-004）；完整 agent turn（带工具 / 子代理）放 M3。

## Q4：存储方案 —— ✅ 已定（2026-08-20）
- **决议**：`storage-domain`（`defineDomain` + zod 校验，官方 `message-feedback` 同款，ADR-005）。

## Q5：抽屉作用域 —— ✅ 已定（2026-08-20）
- **决议**：**全局抽屉**（跨会话共享，ADR-006）。

## Q6：追问结果是否流式 —— 暂定（按推荐）
- **决议**：MVP 一次性返回；流式打字机放 M2。（未单独确认，可随时改）

## Q7：入口方式 —— 暂定（按推荐）
- **决议**：选区浮钮（核心）+ 浮窗内按钮；快捷键 M3。（未单独确认，可随时改）

---

## 确认记录

- 2026-08-20：Q2~Q5 用户确认；Q6/Q7 暂按推荐。全部待决项清零，可进入 M0 脚手架。
